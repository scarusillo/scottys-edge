"""Per-step modules for the cmd_run pipeline.

Each module corresponds to a numbered step from the original cmd_run god
function. Extracted from main.py in v26.0 Phase 8 (cmd_run decomposition).

Stage modules:
    data_refresh.py — Steps 1-5b: injuries + odds + props + pitcher + goalie + ref + ratings + Elo
"""
